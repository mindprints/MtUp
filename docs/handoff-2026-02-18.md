# Handoff - 2026-02-18

## Session Summary

This session established the first end-to-end AI interaction baseline in `mtUp`, moved the authenticated landing experience to AI-first, and hardened proposal creation and conversational follow-ups in Supabase mode.

## Completed Work

### 1) AI-First App Surface

- Added a primary assistant/workspace shell:
  - `src/components/PrimaryExperience.tsx`
- Default post-login experience now opens the assistant tab when AI is enabled.
- Added persistent tab preference:
  - localStorage key: `mtup-primary-tab`
- Updated app composition:
  - `src/App.tsx`

### 2) Assistant UI + Client Runtime

- Added reusable assistant panel:
  - `src/components/AiAssistantPanel.tsx`
- Added typed AI contracts:
  - `src/types/ai.ts`
  - `src/types/index.ts` export
- Added AI client:
  - `src/lib/aiClient.ts`
  - timeout and retry behavior for slower models
  - clearer error messages for timeout/connectivity
- Added unit test coverage:
  - `src/lib/aiClient.test.ts`

### 3) Runtime Config and Startup Telemetry

- Added AI feature/runtime config:
  - `VITE_AI_ASSISTANT_ENABLED`
  - `VITE_ORCHESTRATOR_BASE_URL`
  - `src/lib/runtimeConfig.ts`
  - `src/lib/runtimeConfig.test.ts`
- Added startup diagnostics:
  - `src/main.tsx`

### 4) Local Dev Orchestrator + OpenRouter Coupling

- Added local orchestrator server:
  - `server/dev-orchestrator.mjs`
  - endpoints:
    - `POST /ai/chat`
    - `GET /health`
- Added package script:
  - `npm run ai:dev`
- Added env auto-loading for orchestrator:
  - reads `.env` and `.env.local` automatically
- Added OpenRouter support and fallback behavior:
  - model-backed intent classification/polish
  - deterministic tool output for core intents
  - fallback when OpenRouter key unavailable

### 5) Deterministic Read-Only Tool Coverage

Implemented read-only behaviors in orchestrator:

- confirmed activities query
- “my availability” query
- “this week” date-window handling (deterministic)
- attendee follow-up context (`who is coming?`) with thread memory

### 6) Proposal Creation and UI/Filter Improvements

- Proposal creation hardening in Supabase mode:
  - fallback membership lookup if group context has not fully resolved
  - `src/lib/ProposalContext.tsx`
- Updated create entry UX:
  - `+ Event` and `+ Sejour`
  - `src/components/AppView.tsx`
  - `src/components/CreateProposalModal.tsx`
- Calendar visualization differentiation:
  - type-aware markers/styling for event vs sejour
  - `src/components/CalendarCell.tsx`
  - `src/components/IndividualCalendar.tsx`

## Testing / Verification

- `npm run test:run`: passing
- `npm run build`: passing
- `npm run test:e2e`: passing (`8 passed`, `1 skipped`) after test adaptation for AI-first shell

## Docs Updated

- `docs/current-architecture-truth.md`
- `docs/execution-tracker-2026-02-17.md`
- `docs/plan-5-ai-implementation-backlog.md`
- `docs/terminology-taxonomy.md`
- `README.md` (AI/orchestrator setup and updated feature state)

## Known Gaps

1. AI remains read-only for writes/actions.
2. No AI audit-log tables/workflow yet.
3. Follow-up conversation quality still needs broader scripted scenario hardening.
4. Rich visual cards (maps/images/venue cards) are not implemented yet.

## Recommended Next Steps (Tomorrow)

1. Run scripted conversation suite from user-provided scenarios and log failure classes.
2. Add action proposal cards (approve/deny UI) for first self-write action:
   - remove my availability for a week range.
3. Add orchestrator turn metadata in responses (intent/tool/row counts) behind debug flag.
4. Start visual answer block scaffolding (map/venue/weather cards) with deterministic payload contracts.
