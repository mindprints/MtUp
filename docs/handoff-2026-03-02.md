# Handoff - 2026-03-02

## Session Summary

This session moved the Snooky proposal feed into a fixed phone-first, full-screen card flow and tightened the card UX for decision-making.

The experience now behaves like a swipe/scroll activity deck: one activity card fills the phone viewport, with snap scrolling between cards in calendar order.

## Completed Work

### 1) Device Preview Removed, Phone Shell Standardized

- Removed the phone/tablet/desktop chooser.
- Locked runtime shell to a fixed portrait phone simulation.
- Updated the shell to approximately `19.5:9` ratio.
- Constrained scrolling to in-device content only.
- Removed visible scrollbar for the card deck while preserving scroll/swipe behavior.
- Files:
  - `src/components/Dashboard.tsx`
  - `src/components/PrimaryExperience.tsx`
  - `src/index.css`

### 2) Full-Screen Activity Card Deck

- Proposal cards now render as full-height snap cards (`snap-y snap-mandatory`).
- Each card fills the available screen area and advances one-by-one on scroll.
- Card ordering changed to calendar-first:
  - sort by earliest proposal date when available
  - fallback to `createdAt` ordering
- File:
  - `src/components/AiAssistantPanel.tsx`

### 3) Card Information Architecture Refactor

- Removed from cards:
  - `proposer auto-in` notice
  - `You're in` badge
  - `Generate Thumbnail` button
  - `Crew` dropdown
  - `Changes` dropdown
- Kept and repositioned:
  - `Suggest Alternatives` moved to bottom action area
- Added always-visible structured details:
  - Date / Time / Place
  - Proposer notes
  - Requirements
  - Suggested alternatives list with checkboxes
- File:
  - `src/components/AiAssistantPanel.tsx`

### 4) Group Roster Display Clarified

- Initials row now renders full roster consistently for the planning context:
  - Alice, Bob, Charlie, Denise, Eve
- Subscription state is now clearly demarcated via avatar treatment and `IN` badges.
- Added compatibility mapping for older `Diana`-named local seed data.
- File:
  - `src/components/AiAssistantPanel.tsx`

### 5) 24-Hour Time Display

- Added 24-hour formatting normalization for displayed proposal times and alternative times.
- Updated phone status bar mock time to `09:41`.
- Files:
  - `src/components/AiAssistantPanel.tsx`
  - `src/components/Dashboard.tsx`

### 6) Mock Data Fill for Alice Lunch Proposal

- Added contextual mock fill (when Alice lunch proposal is present) to validate layout:
  - baseline date/time/place
  - proposer notes
  - requirements
- Added fallback mock alternatives if not already present:
  - Eve (`E`) alternative date/time
  - Charlie (`C`) alternative place
- Alternative checkboxes are now wired to local component state.
- File:
  - `src/components/AiAssistantPanel.tsx`

### 7) Popup Calendar for Date Comparison

- Added per-card `Calendar` button opening a modal calendar.
- Calendar displays:
  - original proposal dates (blue)
  - alternative dates (amber)
  - overlap style when both exist
- Includes legend in popup.
- Files:
  - `src/components/AiAssistantPanel.tsx`
  - `src/components/Modal.tsx` (reused, no structural changes required)

### 8) Top Header Control Update

- Replaced decorative bicycle icons with a functional `Propose` button.
- Current behavior: routes user to `Workspace` tab for proposal actions.
- File:
  - `src/components/PrimaryExperience.tsx`

### 9) Image Compression Utility (Prep Work)

- Added PNG -> WebP batch conversion script and npm command.
- Script supports thresholding, quality, and max-width resizing.
- Files:
  - `scripts/compress-images.mjs`
  - `package.json`
  - `README.md`
- Note: `sharp` dependency installation was blocked in this environment due to permissions/network restrictions; script is ready but requires local install.

## Verification

- `npm run build` passed after each major UI/data patch, including final calendar popup integration.

## Known Gaps / Follow-Ups

1. Checkbox persistence
- Alternative checkbox selections are currently in component state only.
- Not persisted to local storage or backend yet.

2. Mock lunch content source of truth
- Alice lunch mock fill is currently injected in UI rendering logic for fast layout iteration.
- Should be migrated to explicit seed fixtures/store layer if retained.

3. Propose action path
- `Propose` currently switches to `Workspace`.
- If Snooky-first proposal creation is preferred, this can instead open the in-panel proposal flow directly.

4. Calendar scope
- Calendar popup currently shows one anchor month (derived from first known date).
- Multi-month pagination can be added if needed for sejour-length proposals.

## Suggested First Steps Tomorrow

1. Persist alternative checkbox selections (per user/per proposal).
2. Move mock lunch/alternative seed data into dedicated local seed utilities.
3. Decide whether `Propose` should open Snooky proposal draft directly.
4. Add month navigation to the calendar popup if sejour date ranges require it.
