# Handoff - 2026-03-03

## Session Summary

Today focused on stabilizing the new 3-screen mobile-web UI foundation and hardening the Snooky proposal flow while keeping functionality scoped to proposal creation.

Current primary screens:
- `Activities`
- `Snooky` (proposal-focused flow)
- `Admin`

The work emphasized UI behavior, navigation, and reducing legacy/mocked behavior that could cause duplication or confusion.

## Completed Work

### 1) Propose/Snooky Screen Flow

- Created and wired a dedicated `ProposeScreen`.
- Reworked propose flow to be chat-first:
  - No initial activity card when opening Snooky.
  - Drafted details appear inline directly under Snooky assistant responses.
  - Inline drafted fields are editable.
  - Added `Confirm` action for proposal creation.
- Added bottom action bar behavior:
  - `Confirm`
  - `Cancel`
  - `Activities` (always visible, even after canceling draft)
- Removed top nav row while inside Snooky screen to maximize usable space.

Files:
- `src/components/ProposeScreen.tsx`
- `src/components/AiAssistantPanel.tsx`
- `src/components/PrimaryExperience.tsx`

### 2) Activities Screen Navigation and Header Simplification

- Removed top `Activities` and `Workspace` buttons on Activities view.
- Renamed top entry button from `Propose` to `Snooky`.

File:
- `src/components/PrimaryExperience.tsx`

### 3) Admin Dashboard (New Dedicated Screen)

- Added `AdminDashboard` screen for admin users.
- Added admin route/tab wiring in `PrimaryExperience`.
- Added member admin actions and event deletion controls:
  - Promote/demote member admin role
  - Remove member
  - Add member (local mode)
  - Delete individual events
  - Delete all events
- Added member email display in admin UI.

Files:
- `src/components/AdminDashboard.tsx`
- `src/components/PrimaryExperience.tsx`
- `src/lib/ProposalContext.tsx`
- `src/lib/storage.ts`

### 4) Local Auth and Member Data Improvements

- Local login now accepts either:
  - `name/password`
  - `email/password`
- Added `email?: string` to `User` type.
- Seed/mock users now include `@mtup.local` emails.
- New local members receive generated local email format.

Files:
- `src/types/index.ts`
- `src/lib/storage.ts`
- `src/lib/AuthContext.tsx`
- `src/lib/ProposalContext.tsx`

### 5) Snooky Proposal Reliability + Parsing Fixes

- Added propose-mode context to chat requests (`uiMode: 'propose'`).
- Updated orchestrator intent classification to better keep propose flow in proposal mode and support follow-up refinement turns.
- Fixed place parsing issue where time-like tokens (e.g. `14`, `14:00`) were mistakenly treated as place.
- Added sanitation/fallback so `in ...` locations are still captured if `at ...` is time-like.
- Normalized invitees handling for “everyone/all” phrasing.

Files:
- `src/types/ai.ts`
- `src/components/AiAssistantPanel.tsx`
- `server/dev-orchestrator.mjs`

### 6) Proposal Persistence Fixes

- Fixed confirmed proposal creation to persist:
  - `requirements`
  - `comments`
- These now appear in Activities card sections that read from proposal comments.

File:
- `src/components/AiAssistantPanel.tsx`

### 7) Activities Card Roster + Mock Data Cleanup

- Removed legacy/mock lunch data injection in Activities card rendering.
- Switched roster rendering to use live `groupUsers` when available (fixes missing new members like Frank).

File:
- `src/components/AiAssistantPanel.tsx`

### 8) Readability/UI Tweaks

- Increased Activities info-area typography and line-height.
- Improved word wrapping in Snooky messages and proposal draft fields.
- Converted some Snooky draft fields to multiline textareas (`Invitees`, `Place`, `Requirements`).
- Fixed dark-mode date/time widget contrast in Snooky draft editor (`dark:[color-scheme:dark]`).

File:
- `src/components/AiAssistantPanel.tsx`

### 9) Image Generation + Compression Flow

- Enabled automatic proposal image generation on Snooky confirm (no visible manual button required).
- Added OpenRouter API key fallback logic:
  - If `VITE_THUMBNAIL_OPENROUTER_API_KEY` returns `401`, retry with `VITE_OPENROUTER_API_KEY`.
- Verified WebP compression script works (`scripts/compress-images.mjs`).

Files:
- `src/components/AiAssistantPanel.tsx`
- `src/lib/thumbnailGenerator.ts`

### 10) Calendar Export

- Added `To My Calendar` button on Activities cards.
- Implemented active `.ics` export/download from proposal data.
- Supports timed and all-day style export.

File:
- `src/components/AiAssistantPanel.tsx`

### 11) Codebase Survey for Proposal Logic

- Added survey document to map canonical proposal-related logic and reduce duplication risk as the new UI evolves.

File:
- `docs/proposal-flow-code-survey-2026-03-03.md`

## Verification Performed

- Repeated `npm run build` checks after each major patch; all passed.
- `node --check server/dev-orchestrator.mjs` passed after orchestrator edits.
- WebP script smoke test passed:
  - Converted test PNG to WebP with expected size reduction.
- OpenRouter image API smoke check passed in this environment after approved network call:
  - Confirmed image response payload received.

## Known Gaps / Observations

1. Occasional read-only response in Snooky propose flow
- Message seen:
  - `Read-only mode is active. I can currently answer confirmed activities and your availability.`
- Likely due to orchestrator process staleness or unsupported intent branch firing.
- First troubleshooting step tomorrow:
  - restart `npm run ai:dev` and frontend dev server.

2. Proposal parsing quality is still heuristic + model-dependent
- Some edge phrasing may still require manual field edits.
- Expected at current stage; parsing/richer reasoning intentionally deferred.

3. Direct calendar-provider write is not implemented
- Current `To My Calendar` uses `.ics` (works without OAuth).
- Direct Google/Outlook event creation would require OAuth scopes and integration work.

## Recommended First Steps Tomorrow

1. Stabilize Snooky propose-only behavior
- In `uiMode: 'propose'`, consider forcing unknown intent fallback to proposal drafting instead of read-only guidance.

2. Improve draft field labeling/layout on Snooky screen
- Keep the UI-first focus by making field identity and hierarchy even clearer for quick mobile edits.

3. Add proposal-flow runtime diagnostics
- Lightweight UI/debug marker when orchestrator returns `unsupported` in propose mode to speed issue isolation.

4. Keep proposal logic centralized
- Continue using `ProposalContext` as the only write path for proposal/member mutations.

